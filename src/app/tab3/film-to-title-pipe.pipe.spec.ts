import { FilmToTitlePipePipe } from './film-to-title-pipe.pipe';

describe('FilmToTitlePipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilmToTitlePipePipe();
    expect(pipe).toBeTruthy();
  });
});
